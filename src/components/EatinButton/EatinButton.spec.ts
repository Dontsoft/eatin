import { shallowMount } from "@vue/test-utils";
import EatinButton from "./EatinButton.vue";

describe("EatinButton", () => {
  it("should render text correctly", () => {
    
    const wrapper = shallowMount(EatinButton, {
    });
  });
});